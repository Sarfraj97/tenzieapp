import React from 'react'
import { Login } from './Login';
// import Button from 'react-bootstrap/Button';

export const Header = () => {
    

    return (
        <div><Login handleLogin={handleLogin}/></div>
    )
}